# ENVIRONNEMENT DE PRODUCTION - IKONGA APP
# Ces variables doivent être configurées dans l'interface Dokploy (Environment Variables).
# Copiez ce fichier vers .env.local pour le développement local si nécessaire.

# 1. BASE DE DONNÉES (Supabase)
# Utilisé par Prisma pour les migrations et les requêtes.
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_ID].supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_ID].supabase.co:5432/postgres"

# 2. AUTHENTIFICATION & STOCKAGE (Supabase)
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT_ID].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_anon_key"
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_FOR_SERVER_ONLY"

# 3. INTELLIGENCE ARTIFICIELLE (OpenAI)
OPENAI_API_KEY="sk-...."

# 4. CONFIGURATION NEXT.JS
NEXT_PUBLIC_SITE_URL="https://votre-domaine.com"
NODE_ENV="production"

# 5. AUTRES (Facultatif)
# ANALYTICS_ID="change_me"
CRON_SECRET="generate_a_strong_random_string_here"
